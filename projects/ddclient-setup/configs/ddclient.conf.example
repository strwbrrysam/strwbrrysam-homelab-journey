# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf
syslog=yes              # log the output to syslog
ssl=yes                 # use ssl when updating IP
pid=/run/ddclient.pid
daemon=600
use=web, web=dynamicdns.park-your-domain.com/getip
protocol=namecheap
server=dynamicdns.park-your-domain.com
login=YOURDOMAIN.COM
password=namecheapaccountpassword
YOURHOST

# this config file uses YOURHOST.YOURDOMAIN.COM as an example host and domain. replace it where appropriate. 

#mail=root               # send email notifications to root
#mail-failure=root       # send email when failed
